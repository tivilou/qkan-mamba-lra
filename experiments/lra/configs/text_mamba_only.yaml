task: text
seed: 42

model:
  name: mamba_only
  params:
    d_model: 128
    n_layers: 4
    d_state: 16
    d_conv: 4
    expand: 2
    pooling: mean

training:
  batch_size: 32
  lr: 0.001
  weight_decay: 0.01
  epochs: 20
  num_workers: 2

results_dir: results/text
